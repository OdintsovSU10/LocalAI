# Stage 08 report

Status: IMPLEMENTED — ждёт независимой проверки и запуска с реальным ботом

## Baseline
- Stage 07 PASS (`0cebc25`). Проверенные ответы доступны только в веб-интерфейсе; Telegram-кода в проекте не было.
- Conversation Service (Stage 03) уже поддерживает канал `telegram` и импорт по внешнему идентификатору.

## Changed
- Новое приложение `apps/telegram-bot/` — тонкий адаптер, без своей RAG-логики:
  - `src/config.js` — токен и список разрешённых пользователей только из окружения; адрес портала обязан быть локальным (loopback); без токена или списка бот не стартует;
  - `src/telegram-api.js` — клиент Bot API на исходящем long polling: входящий порт не открывается, вебхук не используется; токен вырезается из любых сообщений об ошибках;
  - `src/local-api.js` — вызовы портала: `/api/chat`, `/api/conversations`, `/api/conversations/import`, `/api/sources`, `/api/settings`, `/api/health`; наружу отдаются только названия и статусы, без путей и ключей;
  - `src/sessions.js` — состояние чата: разговор, закреплённый проект, последние источники, текущий запрос; ограничитель частоты на пользователя;
  - `src/format.js` — сообщение ответа (текст, список источников по меткам цитат, строка статуса проверки), ограниченная выдержка фрагмента, безопасная разбивка длинных сообщений, клавиатуры;
  - `src/handlers.js` — команды `/new`, `/project`, `/sources`, `/status`, `/cancel`, `/help`, обычный вопрос, кнопки уточнения проекта и фрагментов;
  - `src/bot.js`, `src/index.js` — цикл опроса с отступами при ошибках и точка входа.
- `package.json` -> `tg:start`, `tg:check`, `tg:test`, `tg:contract`.
- `README.md` -> раздел о Telegram: настройка, команды, приватность.

## Architecture decisions
- **Адаптер, а не второй backend.** Ответ всегда идёт через `/api/chat` портала, то есть через planner → retrieval → пакет доказательств → проверку Stage 07. В боте нет ни поиска, ни промптов.
- **Исходящий polling.** Локальный компьютер не открывает порт наружу и не нуждается в белом IP. Вебхук не используется.
- **Один чат — один разговор.** Идентификатор `tg-<chatId>` импортируется в Conversation Service, поэтому после перезапуска бота разговор продолжается. `/new` архивирует старый разговор и создаёт новый.
- **Наружу уходит минимум.** Только текст ответа, метки цитат (файл, пункт, лист и строка или страница) и статус проверки. Абсолютных путей, ключей и полного документа в сообщениях нет; выдержка фрагмента ограничена 700 символами и доступна по кнопке, а не автоматически.
- **Незнакомец не узнаёт ничего.** Любое сообщение не из списка получает нейтральный отказ без названий проектов и признаков того, что за ботом.
- **Содержимое не логируется.** В консоль идут только количества, вид ошибки и обрезанный текст ошибки; токен вырезается.

## Acceptance (Prompt Pack Stage 08) -> доказательство
Сквозной сценарий в `apps/telegram-bot/tests/flow.contract.mjs` на реальном портале с фейковой LLM:
1. вопрос про «Сокольники» при двух похожих проектах -> кнопки-кандидаты;
2. выбор кнопкой -> исходный вопрос возобновляется для выбранного проекта;
3. ответ приходит проверенным, со ссылками `[1]` и списком источников;
4. кнопка «Фрагмент 1» -> ограниченная выдержка;
5. follow-up остаётся в том же разговоре;
6. `/new` сбрасывает разговор и закреплённый проект — проект снова уточняется;
7. чужой пользователь получает только «Доступ не настроен.»;
8. в сообщениях нет локальных путей.

Остальное — в `apps/telegram-bot/tests/`: список разрешённых, ограничитель частоты, отмена долгого запроса, команды, разбивка длинных сообщений, приватность `/help`.

## Tests / evidence
Машина разработки: `node --check` всех модулей -> PASS; юнит-кейсы (config/format 9, handlers 7) прогнаны лёгкой пробой -> совпали.

`npm test`, `npm run tg:test`, `npm run tg:contract` здесь не запускались.

На машине владельца (NOT RUN здесь):
- `npm run check`, `npm run tg:check`, `npm test`, `npm run tg:test`, `npm run tg:contract`;
- запуск с реальным ботом: `npm run tg:start` при работающем портале.

## Security/privacy
- `TELEGRAM_BOT_TOKEN` и `TELEGRAM_ALLOWED_USER_IDS` — только из `.env`; в репозитории их нет, в логах тоже.
- Адрес портала обязан быть loopback: бот не может быть перенастроен на удалённый backend.
- Если у портала включён `RAG_AUTH_TOKEN`, бот использует его в заголовке; токен не печатается.
- Ограничение частоты по пользователю, отмена запроса, тайм-аут.
- `/help` прямо предупреждает: документы и индекс остаются локально, но вопрос и ответ идут через серверы Telegram.

## Known limitations
- Уведомления (окончание индексации, найденные расхождения) не реализованы — нужен отдельный источник событий, это Stage 09.
- Состояние чата (последние источники, выбранный проект) живёт в памяти процесса: после перезапуска бота разговор сохраняется, а кнопки «Фрагмент» из старых сообщений перестают работать.
- Групповые чаты не рассчитаны специально: allowlist проверяет пользователя, а не чат.
- Ответ идёт 30–60 секунд, всё это время виден индикатор «печатает».
- Длинный ответ разбивается на части; кнопки прикрепляются к последней.

## Ready for next stage?
NO
Reason: требуется запуск с реальным ботом и независимая проверка.

---

## Revision 1 — после независимой проверки (verdict FAIL)

Проверка подтвердила: этапы 00–07 не сломаны, безопасность и приватность (пункты a–h) проходят целиком, сценарий с кнопками, перезапуском и чужим пользователем работает. Два блокера были в тестовой обвязке, два — в покрытии.

### Findings
1. **[HIGH] `npm run tg:test` падал на Windows.** Каталог в `node --test` не разворачивается в файлы.
   - Исправление: скрипт приведён к тому же виду, что `mcp:test` — `node --test apps/telegram-bot/tests/*.test.mjs`.
2. **[HIGH] `npm run tg:contract` падал на сценарии «уточнение → ответ».** Фейковая LLM брала первое сообщение пользователя, а при истории диалога первым идёт прошлый ход, а не черновик с доказательствами `[E1]`. Черновик получался пустым, ответ — `insufficient_evidence`.
   - Исправление: `tests/helpers/chat-runtime.mjs` берёт последнее сообщение пользователя — это всегда текущий промпт. Проверено сквозной пробой: два хода подряд в одном разговоре дают `verified` с 10%.
   - Дефект был в тестовой обвязке, а не в боте или ядре: любой многоходовой сценарий с фейковой моделью был красным.
3. **[MED] Не было теста, что `/sources` не отдаёт путь.**
4. **[MED] Не было теста, что токен бота не попадает в текст ошибки.**
   - Исправление: новый файл `apps/telegram-bot/tests/api-clients.test.mjs` — поля источника ограничены белым списком (`id`, `title`, `sourceType`, `indexStatus`, `indexedFiles`), в выводе нет путей и ключей из настроек; ошибка Telegram с токеном в тексте и ошибка сети с токеном в URL приводят к `<token>`; необязательные вызовы Telegram не ломают ответ.

### Принято без изменений
- **Кнопка уточнения шлёт название проекта, а не `sourceId`.** Это и есть механизм Stage 05: незавершённое уточнение хранится на сервере, и ответ названием возобновляет исходный вопрос. Если передавать `sourceId`, сервер получит новый вопрос «Балчуг, Садовническая» вместо возобновления исходного. Устойчивость этого пути закрыта тестами планировщика (выбор названием и номером) и сквозным сценарием.

### Changed
- `package.json` (`tg:test`), `tests/helpers/chat-runtime.mjs`, новый `apps/telegram-bot/tests/api-clients.test.mjs`, этот отчёт.

### Tests / evidence
Машина разработки: `node --check` -> PASS; юнит-кейсы бота (config/format 9, handlers 7, api-clients 4) прогнаны лёгкой пробой -> совпали; сквозная проба многоходового диалога через настоящий `llm.js` и фейковую HTTP-LLM -> `verified`.

`npm test`, `npm run tg:test`, `npm run tg:contract` здесь не запускались.

---

## Revision 2 — после повторной проверки (verdict PARTIAL)

Проверка подтвердила: блокеры Revision 1 закрыты, все gates зелёные (`npm test` 458/458, `tg:test` 20/20, `tg:contract` PASS, четыре контракта Stage 02–07 PASS), 8 мутаций из 8 ловятся. Найден один новый дефект.

### Finding
**[HIGH] Бот не понимал ответ портала о проектах.** `GET /api/sources` отдаёт массив, а клиент читал поле `sources`. В результате `/sources` отвечал «В портале пока нет проектов», `/project` не показывал кнопок, а `/status` сообщал 0 проектов. Юнит-тест не поймал: в нём была придуманная обёртка `{ sources: [...] }`.

- Исправление: клиент принимает обе формы — массив (реальную) и обёрнутую; белый список полей не менялся.
- Тест `api-clients.test.mjs` переписан на реальную форму (массив с `path`, `additionalPaths`, `linkedTenders`), добавлен отдельный тест на обёрнутую форму и на пустой ответ.
- Сквозной контракт `flow.contract.mjs` теперь сначала выполняет `/sources`, `/status` и `/project` на живом портале и проверяет, что список из двух проектов пришёл, а локальных путей в сообщении нет. Расхождение формы с API будет падать здесь, а не у пользователя.

### Changed
- `apps/telegram-bot/src/local-api.js`, `apps/telegram-bot/tests/api-clients.test.mjs`, `apps/telegram-bot/tests/flow.contract.mjs`, этот отчёт.

### Tests / evidence
Машина разработки: `node --check` -> PASS; юнит-кейсы api-clients (5) прогнаны лёгкой пробой -> совпали. `npm run tg:test` и `npm run tg:contract` здесь не запускались.

---

## Revision 3 — после проверки с вердиктом PASS

Проверка подтвердила: все gates зелёные (`npm test` 459/459, `tg:test` 21/21, `tg:contract` PASS, контракты Stage 02–07 PASS), формы ответов портала сверены со всеми используемыми роутами, мутации ловятся. Осталась одна рекомендация.

- **[MED] Мутация «отдавать источник целиком» краснела только в юнит-тесте.** Сквозной контракт её не ловил: команды печатают лишь название и счётчики, поэтому путь в текст не попадал. Защита держалась на форматировании, а не на контракте.
  - Исправление: контракт теперь сам вызывает `sources()` на живом портале и проверяет точный набор полей (`id`, `title`, `sourceType`, `indexStatus`, `indexedFiles`), отсутствие корня временного каталога и слова `path` в сериализованном списке.

Остаток этапа — запуск с реальным ботом у владельца.
